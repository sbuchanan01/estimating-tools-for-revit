using System.Windows.Controls;

namespace EstimatingTools.UI
{
    /// <summary>Renders one <see cref="EstimateCard"/> (its DataContext).</summary>
    public partial class EstimateCardView : UserControl
    {
        public EstimateCardView()
        {
            InitializeComponent();
        }
    }
}
